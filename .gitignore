/target
**/target
node_modules
dist
.DS_Store

# Generated by scripts/gen-embedded.sh (base64 delegate WASM + key + registry
# id). The committed embedded.stub.ts is copied in by `prepare` as a fallback.
/client/src/embedded.ts
/site/whitepaper.html
.artifact-build-stamp

# Extracted container build artifacts — reproducible from build/Dockerfile,
# so committing them adds ~800KB of binaries that can silently disagree with
# the source they claim to come from.
build/out/
